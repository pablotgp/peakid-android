<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Ubicación: solo para SEMBRAR la posición cuando la foto no trae GPS
         en su EXIF. La fuente primaria de una foto de galería es su propio
         EXIF, no dónde está el móvil ahora. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- La cámara es opcional: se puede usar la app solo con la galería. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.compass" android:required="false" />

    <application
        android:label="PeakID"
        android:supportsRtl="true"
        android:theme="@style/Theme.PeakID">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="PeakID"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.PeakID">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Para entregar a la cámara del sistema un destino donde escribir. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
